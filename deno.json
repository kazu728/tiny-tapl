{
  "tasks": {
    "build": "elm make src/Main.elm --optimize --output=build/main.js",
    "run": {
      "command": "deno run --quiet --allow-env --allow-sys=cpus --allow-read run.ts",
      "dependencies": ["build"]
    },
    "test": "elm-test"
  }
}
